import { Button } from "./ui/button";
import Cards from "./header-cards/Cards";

const HeroContainer = () => {
  return (
    <div className="bg-main -mt-10 text-white p-16 ml-0" style={{width: 100 + 'vw'}}>
        <h2 className="text-center text-2xl font-semibold">Только planning poker ничего лишнего</h2>
        

    </div>

  );
};

export default HeroContainer;